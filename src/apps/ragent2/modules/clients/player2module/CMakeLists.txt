IF(PLAYER_FOUND)
	RDK_ADD_RAGENT_MODULE(ALL_FILES)
	INCLUDE_DIRECTORIES(${PLAYER_INCLUDE_DIR})
	LINK_DIRECTORIES(${PLAYER_LINK_DIRECTORIES})
	ADD_DEFINITIONS(${PLAYER_DEFINITIONS})
	TARGET_LINK_LIBRARIES(${RDK_THIS_MODULE_NAME} ${PLAYER_LIBRARIES})
ENDIF(PLAYER_FOUND)

